<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/ /DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.totoro.mybatissourcecode.mapper.UserMapper">

    <select id="queryAllList" resultType="com.totoro.mybatissourcecode.entity.UserEntity">
        select id, user_name, real_name, password, age from t_user
    </select>
</mapper>